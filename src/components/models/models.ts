export type fireType = {
  bookingid: string;
  date: string;
  time: string;
  userid: string;
  timeID: string;
  timeValue: string;
};

export type TimeType = {
  date: string;
  bookingid: string;
  time: string;
  userid: string;
};
